{
  "name": "bingego-live-precompute",
  "private": true,
  "type": "module",
  "description": "Cron-script voor Live: pre-WK. Niet nodig voor de webapp zelf (die draait zonder build-step direct uit /js).",
  "scripts": {
    "live-precompute": "node scripts/live-precompute.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
